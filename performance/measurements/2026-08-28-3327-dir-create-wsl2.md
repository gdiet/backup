# Directory creation - native - 3327/WSL2/local SSD

## Setup
- Date: 2026-08-28
- Machine: 3327
- Execution environment: WSL2, Ubuntu 24.04.4 LTS
- Power profile: base scheme `aeeed979-846d-4e51-bb46-e7b4f140eb43` ("Thieme Energy Options", a
  custom/renamed Windows scheme, not one of the stock named profiles), AC power-mode overlay
  `ded574b5-45a0-4f42-8737-46345c09c238` = `OVERLAY_SCHEME_MAX` ("Maximale Leistung hat Vorrang" /
  "Best performance"); DC overlay unset (no battery-specific override). Determined retroactively
  after the original run (see `../methodology.md`'s "Power profile" note for the working
  `reg.exe`/`powercfg /query` procedure) - the developer confirmed the profile had not changed
  since. On AC power (plugged in) during the run, confirmed by the developer - so the AC overlay
  above is the one that actually applied.
- IO device: local SSD (WSL2's own ext4 virtual disk, backed by the host's NVMe SSD per
  `../machines.md`)
- DedupFS build: `46545a96` on `rust-performance-test-idea`
- Isolation: none taken deliberately - ran as the sole foreground command in an otherwise idle WSL2
  session; host-side background activity was not controlled or observed.

## Workload
- Operation: directory creation
- Location: native
- Tool: `mkdir` (WSL2 coreutils), via `performance/scripts/dir-create.sh`
- Mode: sequential
- Window: 20 s
- Scale: ~16,000-21,000 directories per run (see Results); fresh start, no prior state
- Content: not applicable (directories, no file content)

## Results
| Run | Result |
|---|---|
| 1 (discarded?) | 16379 dirs, 20s, 818 ops/s - not discarded: only ~20% below the median of runs 2-5, short of the fixed 50%-slower threshold |
| 2 | 20433 dirs, 20s, 1021 ops/s |
| 3 | 20974 dirs, 20s, 1048 ops/s |
| 4 | 20203 dirs, 20s, 1010 ops/s |
| 5 | 20526 dirs, 20s, 1026 ops/s |

Mean: 984.6 ops/s. Range: 818 - 1048 (N=5)

## Notes

First measurement taken on `3327` at all - closes a full-coverage gap (previously `julius`-only).
`dir-create.sh` itself needed no changes to run cleanly here; treated this run as the script's own
validation on a second machine/distro (Ubuntu 24.04 vs. `julius`'s Debian 12) in addition to being
a real measurement, per this session's own instructions - no problems found.

Power profile's overlay was not captured live during the original 5-minute session (wrong
`powercfg` switch, no time to look it up) - determined correctly afterward instead of left
incomplete; see `../methodology.md`'s "Power profile" note, updated at the same time with the
working procedure this found.
