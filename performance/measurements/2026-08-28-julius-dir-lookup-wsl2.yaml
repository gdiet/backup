date: 2026-08-28
machine: julius
environment: wsl2
power_profile: power-saver             # Balanced base scheme, OVERLAY_SCHEME_MIN overlay on the Windows host, WSL2 shares it
io_device: local-ssd
dfs_build: n/a (native filesystem baseline, no DedupFS code exercised)
operation: dir-lookup
location: native
tool: test -d (WSL2 coreutils/bash builtin)
mode: sequential
parallel_n:
window_seconds: 20
scale: 100000                     # fixed lookup-tree size (directories), built once, not per-run
content_size:
result:
  unit: ops_per_second
  mean: 50350.2
  range: [42929, 55676]
  n: 5
  discarded_warmup_run: false
