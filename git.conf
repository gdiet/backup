git config --local -l
remote.origin.url=https://github.com/gdiet/backup

// Linux and Windows
git config --local user.email gdiet@users.noreply.github.com
git config --local user.name georg

// Windows
git config --local core.autocrlf input

// core.filemode can be default true as long as access is pure Windows or pure Linux
